server:
  port: 8761

spring:
  application:
    name: discovery-server
  cloud:
    config:
      import-check:
        enabled: false

    compatibility-verifier:
      enabled: false
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false